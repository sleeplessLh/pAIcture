// Sites reserves the top-level /mcp path. Keep the protocol implementation in
// one place and expose it through the supported dynamic API namespace.
export { GET, OPTIONS, POST } from "../../mcp/route";
