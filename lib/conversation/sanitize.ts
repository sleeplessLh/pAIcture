const allowedTags = new Set(["p","br","strong","b","em","i","u","s","ul","ol","li","blockquote","a","pre","code","table","thead","tbody","tr","th","td","h1","h2","h3","h4","hr","img","figure","figcaption","span"]);
export function sanitizeHtml(input: string) { return input.replace(/<script[\s\S]*?<\/script>/gi, "").replace(/<style[\s\S]*?<\/style>/gi, "").replace(/\son\w+\s*=\s*("[^"]*"|'[^']*')/gi, "").replace(/javascript:/gi, "").replace(/<\/?([a-z0-9-]+)(?:\s[^>]*)?>/gi, (tag, name) => allowedTags.has(String(name).toLowerCase()) ? tag : ""); }
export function textToHtml(text: string) { const escaped = text.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;"); return `<p>${escaped.replace(/\n{2,}/g, "</p><p>").replace(/\n/g, "<br>")}</p>`; }
